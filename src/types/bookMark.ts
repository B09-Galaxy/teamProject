export type TSupaInputBookMark = {
  bookMarkId: string;
  departurePlace: string;
  arrivalPlace: string;
  departureTime: string;
  arrivalTime: string;
  charge: number;
  detailType: string;
  transportType: string;
  userId: string;
};
